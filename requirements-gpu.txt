# ==============================================================================
# GPU Dependencies (install INSTEAD of paddlepaddle CPU)
# ==============================================================================
# Usage: pip install -r requirements-gpu.txt

# Replace CPU PaddlePaddle with GPU version
paddlepaddle-gpu==2.6.2       # CUDA 11.8 or 12.x

# Same as requirements.txt
paddleocr>=2.7.0
opencv-python-headless>=4.8
Pillow>=10.0
numpy>=1.24
PyMuPDF>=1.23
gradio>=4.0
psutil>=5.9
PyYAML>=6.0
pytest>=7.0

# GPU monitoring
pynvml>=11.5                  # NVIDIA GPU monitoring
